{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "next build",
  "installCommand": "rm -f pnpm-lock.yaml yarn.lock package-lock.json && corepack enable && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
